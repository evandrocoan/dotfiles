#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
TARGET="${SCRIPT_DIR}/../lib/node_modules/excalidash-mcp/dist/index.js"

exec node "${TARGET}" "$@"
