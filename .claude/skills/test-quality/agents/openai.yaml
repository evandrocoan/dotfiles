interface:
  display_name: "Test Quality"
  short_description: "Create, run, and audit trustworthy tests"
  default_prompt: "Use $test-quality to create, run, or audit these tests so they validate the intended behavior."
