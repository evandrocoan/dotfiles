interface:
  display_name: "Plan Implementation"
  short_description: "Create visible, durable execution plans"
  default_prompt: "Use $plan-implementation to create a visible, persistent, testable execution plan for this change."
