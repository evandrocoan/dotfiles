# Sourced by grub-mkconfig. Enable delay accounting before user-space tasks start.
case " ${GRUB_CMDLINE_LINUX-} " in
    *" delayacct "*) ;;
    *) GRUB_CMDLINE_LINUX="${GRUB_CMDLINE_LINUX:+${GRUB_CMDLINE_LINUX} }delayacct" ;;
esac
